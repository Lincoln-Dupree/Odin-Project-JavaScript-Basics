"use strict";

// Replace the strings to the right of the = with the expression they describe. 
// Do not manually enter the answers to the equation.

// Freebie!!! This is the answer to "one minus one"
const a = 1 - 1 

// "one plus eight"
const b = 1 + 8; 

// "22 times three"
const c = 22 * 3;

// "the *remainder* of 5/4"
const d = 5 % 4;

// "the variable 'b' minus 17"
const e = b - 17;

// "the sum of the previous five variables"
const f = a + b + c + d + e

const answersArray = [a, b, c, d, e, f];

console.log(answersArray);